import { Circle } from "better-react-spinkit"

const Login = () => {

    return (
        <center style={{width: "100%", height: "100%", display: "flex", justifyContent: "center", alignItems: "center"}}>
            <Circle color="rgba(133, 203, 248, 1)" size={60} />
        </center>
    );
}
 
export default Login;
